<div class="text-center text-lg font-mono">
    <span class="font-bold">{{ $vote->argumentsWithBodyAndYes()->count() }}</span> arguments
    for, <span class="font-bold">{{ $vote->argumentsWithBodyAndNo()->count() }}</span> arguments
    against.
</div>
